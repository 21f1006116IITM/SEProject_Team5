// main.js
// Import createApp from the 'vue' package.
// This assumes 'vue' is installed as a dependency (e.g., via npm or yarn).
import { createApp } from 'vue';

// Import your Vue Single File Component
import App from './App.vue'; // Adjust path if components are elsewhere
import './assets/style.css';

// Create and mount the Vue application
createApp(App).mount('#app');
